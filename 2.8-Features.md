# 🚀 Assignment 2.8 – Collaboration Features (Prep Notes)

These are pieces of code already started in **2.6** but meant for **2.8**.

- will “turn them on” and connect them to sockets / shared state

---

## Features Already Started

### 1. User Activity Tracking

- **File(s):** Main component(s) with `useEffect` navigation or `AppState` listeners.
- **Code:** `setUserActivity`
- **Purpose:** Tracks when a user goes active/inactive so collaborators can see status.

---

### 2. Participants State

- **File(s):** Task component / context.
- **Code:** `const [participants, setParticipants] = useState([])`
- **Purpose:** Stores list of users in the session (needed for showing collaborators).

---

### 3. Edit Locks

- **File(s):** Task manager.
- **Code:**
  - `requestEditLock(taskId)`
  - `releaseEditLock(taskId)`
- **Purpose:** Prevents two users editing the same task at once.

---

### 4. Placeholder Comments

- **Examples in Code:**
  - `// Add a new collaborative task`
  - `// set initial state on mount`
- **Purpose:** Reminders for where socket/event-driven code will be added.

---

## 📌 Next Steps When Doing 2.8

1. Connect to real-time layer (e.g., WebSockets, Firebase, or SignalR).
2. Broadcast `participants` list + activity state.
3. Sync task edits with `editLocks` to prevent conflicts.
4. Display collaborator presence + editing indicators in UI.
5. Need to add Chat Rooms
